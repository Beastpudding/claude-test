# Copyright (c) Jupyter Development Team.
# Distributed under the terms of the Modified BSD License.

# JupyterHub docker compose configuration file

services:
  hub:
    build:
      context: .
      dockerfile: Dockerfile.jupyterhub
      args:
        # 폐쇄망: 내부 PyPI 미러 주소로 설정
        PIP_INDEX_URL: ${PIP_INDEX_URL:-}
        PIP_TRUSTED_HOST: ${PIP_TRUSTED_HOST:-}
    restart: always
    image: jupyterhub:local
    container_name: jupyterhub
    networks:
      - jupyterhub-network
    volumes:
      # JupyterHub 설정 파일
      - "./jupyterhub_config.py:/srv/jupyterhub/jupyterhub_config.py:ro"
      # Docker 소켓 (컨테이너에서 Docker daemon 접근)
      - "/var/run/docker.sock:/var/run/docker.sock:rw"
      # JupyterHub 데이터 (DB, cookie secrets)
      - "jupyterhub-data:/data"
    ports:
      - "${JUPYTERHUB_PORT:-8000}:8000"
    environment:
      # JupyterHub 관리자 사용자명
      JUPYTERHUB_ADMIN: ${JUPYTERHUB_ADMIN:-admin}
      # Docker 네트워크 이름
      DOCKER_NETWORK_NAME: jupyterhub-network
      # 사용자 노트북 이미지
      DOCKER_NOTEBOOK_IMAGE: ${DOCKER_NOTEBOOK_IMAGE:-codeserver-kbdev:local}
      # 노트북 디렉토리
      DOCKER_NOTEBOOK_DIR: /home/jovyan/work
      # Proxy Auth Token — 재배포 시 기존 사용자 세션 유지
      CONFIGPROXY_AUTH_TOKEN: ${CONFIGPROXY_AUTH_TOKEN:-}
      # 폐쇄망 PyPI 미러 — .env에 설정 시 singleuser 컨테이너에 전달됨
      PIP_INDEX_URL: ${PIP_INDEX_URL:-}
      PIP_TRUSTED_HOST: ${PIP_TRUSTED_HOST:-}
      # Claude Code OAuth 리다이렉션용 외부 URL — VSCODE_PROXY_URI 생성에 사용
      JUPYTERHUB_EXTERNAL_URL: ${JUPYTERHUB_EXTERNAL_URL:-http://localhost:8000}

volumes:
  jupyterhub-data:

networks:
  jupyterhub-network:
    name: jupyterhub-network
    # 기존 네트워크가 있으면 재사용, 없으면 생성
    external: false
